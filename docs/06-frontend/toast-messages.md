# Toast Messages

I toast devono comparire in basso a sinistra.

## Tipi

- `success`
- `error`
- `info`

## Regola

Usare `AppDialog.message()` come fallback se il plugin toast non e disponibile.

Non usare `alert()` nativo del browser.
