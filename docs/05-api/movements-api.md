# Movements API

## Endpoint

- `GET /movements`
- `GET /movements-kpi`
- `POST /movements`
- `PUT /movements`
- `DELETE /movements`

## Note

Scrittura riservata ad admin.

La cancellazione richiede ruolo `admin` e permesso `movements.delete`.
